# TODO / 백로그

배포 후·확장 단계에서 다룰 항목. (v1 관악구 완성품에는 불필요)

## 크라우드소싱 제보폼 (P1, 배포 후)

유저가 직접 정보를 채우거나 고치도록 하는 입력 UI. 245개 지역구를 혼자 못 채우므로
자생적 확장 엔진이 된다. `tip.status='pending'` 으로 쌓고, 관리자 승인 시 노출.

- [ ] "우리 동네 추가해주세요" — 미등록 지역구 요청 수집
- [ ] "이 수수료 틀렸어요" — 값 오류 신고 (조례 개정 대응)
- [ ] "이런 꿀팁 있어요" — 로컬 팁 제보 → `tip` 테이블
- [ ] 관리자 모더레이션 화면(pending → approved/rejected)
- [ ] 제보 insert 용 RLS 정책(anon insert 제한적 허용) 추가

> 순서상 **배포(유저 확보) → 제보폼**. 봐줄 사람이 생긴 뒤에 의미가 있다.

## 기타 백로그

- [ ] 수수료 신선도: `source_updated_at` 기준 주기 갱신(조례 변동 추적) cron
- [ ] 다지역 라우팅: `/[slug]` 로 지역별 페이지 분리 + 지역 선택 UI 활성화
- [ ] 링크 헬스체크: `report_url` 200 여부 주기 점검(link rot 방지)
